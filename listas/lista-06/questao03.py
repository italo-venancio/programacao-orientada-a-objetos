def copi_cola(arq1, arq2):
    with open(arq1) as leitura:
        with open(arq2, "w") as saida:
            for linha in leitura:
                if (// in linha):
                    pass()
                else :
                    print(linha, file = saida)